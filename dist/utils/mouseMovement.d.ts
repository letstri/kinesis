import { ElementRect } from '../models';
export declare const mouseMovement: ({ target, event, }: {
    target: ElementRect;
    event: MouseEvent;
}) => {
    x: number;
    y: number;
    target: ElementRect;
};
